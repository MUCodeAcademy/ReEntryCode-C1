# Login Page Steps

1. Look at the login page in our store as an example.
2. Will need to create UserContext (will be basically the same as the store's UserContext)
3. Wrap the UserContext.Provider in the index.js
4. Need inputs for the user to enter a username/password
5. Need a button for logging in and a button for registering (those can just do the same thing for now)